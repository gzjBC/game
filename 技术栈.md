# 《High》技术栈与环境检查

> **每次开发前，先运行环境检查**：双击根目录 `check-env.bat`（或 PowerShell 里运行 `.\check-env.ps1`）。
> 全部 PASS 再开始写代码；有 FAIL 按本文件第三节修复后重跑。

## 一、技术栈总览（v1 定稿，详见 docs/01_技术选型.md）

| 层 | 工具 | 版本要求 | 用途 |
| --- | --- | --- | --- |
| 运行时 | Node.js | ≥ 20 LTS | JS 运行环境 |
| 包管理 | npm | 随 Node 安装 | 依赖安装 |
| 版本控制 | Git | ≥ 2.x | 代码版本管理 |
| 构建 | Vite | 5.x | 开发服务器 / 打包 |
| 语言 | TypeScript | 5.x | 类型安全 |
| UI 面板 | React | 18.x | 资源条 / 菜单 / 科技树 |
| 状态管理 | Zustand | 4.x | 游戏状态 |
| 样式 | Tailwind CSS | 3.x | UI 样式 |
| 场景引擎 | Phaser | 3.x | 地图 / 小人动画 / 相机 |
| 寻路 | easystarjs | 0.6.x | 网格 A* |
| 大数字 | break_infinity.js | 2.x | 挂机数值（1e100+） |
| 测试 | Vitest | 2.x | 单元测试（随 Vite） |

## 二、环境检查清单（每次开始前必做）

运行 `check-env.bat` 后逐项确认：

- [ ] Node.js 已安装且版本 ≥ 20（`node -v`）
- [ ] npm 可用（`npm -v`）
- [ ] Git 已安装（`git --version`）
- [ ] 项目结构存在（`游戏开发组\src\package.json`）
- [ ] 依赖已安装（`node_modules` 存在）
- [ ] 关键依赖齐全（phaser / zustand / tailwindcss / easystarjs / break_infinity.js）
- [ ] 项目可启动（`npm run dev` → localhost:5173）

## 三、环境安装指引

### Node.js（未安装或版本 < 20）
1. 官网下载 LTS 版：https://nodejs.org/
2. 一路默认安装，装完重开终端。
3. 推荐用 nvm-windows 管理版本：https://github.com/coreybutler/nvm-windows

### Git
1. 下载安装：https://git-scm.com/
2. 默认选项即可。

### VS Code 插件
- ESLint、Prettier、Tailwind CSS IntelliSense

### 项目依赖（首次或 node_modules 缺失）
```
cd 游戏开发组\src
npm install
```

## 四、常见问题与修复

| 现象 | 原因 | 修复 |
| --- | --- | --- |
| 'node' 不是内部或外部命令 | Node 未装或未加入 PATH | 重装 Node，勾选 Add to PATH |
| npm install 报权限错误 | Windows 权限限制 | 以管理员身份运行终端再试 |
| 端口 5173 被占用 | 其他程序占用 | `npm run dev -- --port 5174` |
| npm ls 报 missing | 依赖缺失 | 重新 `npm install` |

## 五、版本约定

- 依赖版本以 `package.json` 锁定（^ 前缀）；升级前在 `项目记忆.md` 决策记录中登记。
- 升级 Node 大版本前先跑 `check-env.bat` 确认兼容。